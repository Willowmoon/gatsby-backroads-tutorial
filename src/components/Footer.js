import React from "react"
import styles from "./footer.module.scss"

const Footer = () => {
  return (
    <div className={styles.footer}>
      <h1>I'm Footer</h1>
      <div className={styles.links}>here too</div>
    </div>
  )
}

export default Footer
